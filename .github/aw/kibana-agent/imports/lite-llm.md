---
description: Shared POC fragment — LiteLLM / gateway usage notes (stub).
---

## LiteLLM (stub)

- Model calls go through the repository LiteLLM gateway pattern minted in workflow `steps` (see workflow frontmatter). Do not embed API keys in instructions.
